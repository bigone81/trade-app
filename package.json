{
  "name": "trade-app-v2",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.17.1",
  "engines": {"node": ">=24"},
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel --filter @trade/api --filter @trade/web --filter @trade/worker dev",
    "test": "pnpm -r test",
    "migrate:legacy": "node scripts/migrate-legacy.mjs",
    "db:init": "node scripts/init-db.mjs"
  }
}
