import{X}from'lucide-react';import{useI18n}from'../i18n';
export default function ConfirmDialog({open,title,body,danger=false,confirmLabel,onConfirm,onClose}:{open:boolean;title:string;body:React.ReactNode;danger?:boolean;confirmLabel?:string;onConfirm:()=>void|Promise<void>;onClose:()=>void}){const{t}=useI18n();if(!open)return null;return <div className="modal-backdrop" onMouseDown={onClose}><div className="modal" onMouseDown={e=>e.stopPropagation()}><button className="icon-btn modal-x" onClick={onClose}><X size={18}/></button><h3>{title}</h3><div className="modal-body">{body}</div><div className="modal-actions"><button className="btn ghost" onClick={onClose}>{t('Cancel')}</button><button className={danger?'btn danger':'btn primary'} onClick={()=>void onConfirm()}>{confirmLabel||t('Confirm')}</button></div></div></div>}
