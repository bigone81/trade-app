import{NavLink,Outlet}from'react-router-dom';import{Activity,AlertTriangle,BookOpen,ChartCandlestick,Settings,WalletCards}from'lucide-react';import NotificationCenter from'./NotificationCenter';
const items=[['/',ChartCandlestick,'Chart'],['/trade',WalletCards,'Trade'],['/alerts',AlertTriangle,'Alerts'],['/journal',BookOpen,'Journal'],['/settings',Settings,'Settings']] as const;
export default function Layout(){return <div className="app-shell"><aside className="sidebar"><div className="brand"><Activity size={20}/><span>TRADE</span><b>v2</b></div><nav>{items.map(([to,Icon,label])=><NavLink key={to} to={to} end={to==='/'} className={({isActive})=>isActive?'nav-link active':'nav-link'}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-foot"><span className="status-dot"/>private terminal</div></aside><main className="main"><NotificationCenter/><Outlet/></main></div>}
