import{useState}from'react';import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{Bell,Trash2}from'lucide-react';import type{AlertRecord}from'@trade/shared';import{api,json,num}from'../api';
export default function AlertsPage(){const qc=useQueryClient();const[symbol,setSymbol]=useState('BTCUSDT');const[price,setPrice]=useState('');const[condition,setCondition]=useState<'touch'|'cross_up'|'cross_down'>('touch');const[pre,setPre]=useState('.25');const q=useQuery<AlertRecord[]>({queryKey:['alerts-all'],queryFn:()=>api('/api/alerts')});const refresh=()=>void qc.invalidateQueries({queryKey:['alerts-all']});const add=useMutation({mutationFn:()=>api('/api/alerts',json('POST',{symbol,price:Number(price),condition,preAlertPercent:pre===''?null:Number(pre),telegramEnabled:true,triggerOnce:true})),onSuccess:()=>{setPrice('');refresh()}});const toggle=useMutation({mutationFn:(a:AlertRecord)=>api(`/api/alerts/${a.id}/active`,json('PATCH',{active:!a.active})),onSuccess:refresh});const del=useMutation({mutationFn:(id:number)=>api(`/api/alerts/${id}`,{method:'DELETE'}),onSuccess:refresh});return <div className="page"><div className="page-head"><div><h1>Alerts</h1><p>Persistent price alerts watched by the Telegram worker</p></div></div><div className="panel-grid"><div className="card form-card"><h3 style={{marginTop:0}}><Bell size={16}/> New alert</h3><div className="field"><label>Symbol</label><input className="input" value={symbol} onChange={e=>setSymbol(e.target.value.toUpperCase())}/></div><div className="field"><label>Price</label><input className="input" type="number" step="any" value={price} onChange={e=>setPrice(e.target.value)}/></div><div className="field"><label>Condition</label><select className="select" value={condition} onChange={e=>setCondition(e.target.value as any)}><option value="touch">Touch / cross</option><option value="cross_up">Cross up</option><option value="cross_down">Cross down</option></select></div><div className="field"><label>Pre-alert distance %</label><input className="input" type="number" step="0.05" value={pre} onChange={e=>setPre(e.target.value)}/></div><button className="btn primary" style={{width:'100%'}} disabled={!price||add.isPending} onClick={()=>add.mutate()}>Create alert</button></div><div className="card list-card">{q.data?.map(a=><div className="alert-row" key={a.id}><button className={a.active?'toggle on':'toggle'} onClick={()=>toggle.mutate(a)}><span/></button><div className="alert-main"><strong>{a.symbol} · {num(a.price,8)}</strong><small>{a.condition} · pre {a.preAlertPercent??'—'}% · {a.sourceType}{a.triggeredAt?` · triggered ${a.triggeredAt}`:''}</small></div><span className={a.telegramEnabled?'badge live':'badge'}>Telegram</span><button className="icon-btn" onClick={()=>del.mutate(a.id)}><Trash2 size={14}/></button></div>)}{!q.data?.length&&<div className="empty">No alerts yet. Add one here or click Alert on the chart.</div>}</div></div></div>}
