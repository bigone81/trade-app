import{useMemo,useState}from'react';import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{AlertTriangle,RefreshCw,X}from'lucide-react';import type{AccountPublic,TradeExecution,TradeOrder,TradePosition}from'@trade/shared';import{api,json,money,num}from'../api';import ConfirmDialog from'../components/ConfirmDialog';
type Tab='positions'|'orders'|'executions'|'history';
type Action={kind:'cancel';order:TradeOrder}|{kind:'cancelAll';accountId:number;accountName:string;symbol:string}|{kind:'close';position:TradePosition;percent:number}|{kind:'flatten';position:TradePosition}|null;
export default function TradePage(){const qc=useQueryClient();const[account,setAccount]=useState(0);const[symbol,setSymbol]=useState('');const[tab,setTab]=useState<Tab>('positions');const[action,setAction]=useState<Action>(null);const[selected,setSelected]=useState<TradePosition|null>(null);const[sl,setSl]=useState('');const[tp,setTp]=useState('');const[trailing,setTrailing]=useState('');const config=useQuery<{accounts:AccountPublic[];liveTradingEnabled:boolean}>({queryKey:['config'],queryFn:()=>api('/api/config')});const suffix=account?`?accountId=${account}`:'';const summary=useQuery<any[]>({queryKey:['trade-summary',account],queryFn:()=>api(`/api/trade/summary${suffix}`),refetchInterval:10000});const positions=useQuery<TradePosition[]>({queryKey:['positions',account],queryFn:()=>api(`/api/trade/positions${suffix}`),refetchInterval:7000});const orders=useQuery<TradeOrder[]>({queryKey:['orders',account],queryFn:()=>api(`/api/trade/orders${suffix}`),refetchInterval:7000});const executions=useQuery<TradeExecution[]>({queryKey:['executions',account],queryFn:()=>api(`/api/trade/executions${suffix}`),refetchInterval:12000});const history=useQuery<TradeOrder[]>({queryKey:['trade-history',account],queryFn:()=>api(`/api/trade/history${suffix}`),refetchInterval:30000});const refresh=()=>void qc.invalidateQueries({queryKey:['trade-summary']}).then(()=>qc.invalidateQueries({queryKey:['positions']})).then(()=>qc.invalidateQueries({queryKey:['orders']}));
 const run=useMutation({mutationFn:async(a:NonNullable<Action>)=>{if(a.kind==='cancel')return api('/api/trade/orders/cancel',json('POST',{accountId:a.order.accountId,symbol:a.order.symbol,orderId:a.order.orderId}));if(a.kind==='cancelAll')return api('/api/trade/orders/cancel-all',json('POST',{accountId:a.accountId,symbol:a.symbol}));if(a.kind==='close')return api('/api/trade/position/close',json('POST',{accountId:a.position.accountId,symbol:a.position.symbol,percent:a.percent,positionIdx:a.position.positionIdx}));return api('/api/trade/position/flatten',json('POST',{accountId:a.position.accountId,symbol:a.position.symbol,positionIdx:a.position.positionIdx}))},onSettled:()=>{setAction(null);setTimeout(refresh,700)}});
 const updateStops=useMutation({mutationFn:(override:{stopLoss?:number;takeProfit?:number;trailingStop?:number}={})=>selected?api('/api/trade/position/stops',json('POST',{accountId:selected.accountId,symbol:selected.symbol,positionIdx:selected.positionIdx,stopLoss:override.stopLoss??(Number(sl)||0),takeProfit:override.takeProfit??(Number(tp)||0),trailingStop:override.trailingStop??(Number(trailing)||0)})):Promise.resolve(),onSuccess:()=>{setTimeout(refresh,700)}});
 const filter=<T extends{symbol:string}>(rows:T[]|undefined)=>rows?.filter(r=>!symbol||r.symbol.includes(symbol.toUpperCase()))||[];const live=config.data?.liveTradingEnabled??false;
 const tabs:[Tab,string,number][]=[['positions','Positions',positions.data?.length||0],['orders','Orders',orders.data?.length||0],['executions','Executions',executions.data?.length||0],['history','History',history.data?.length||0]];
 const actionBody=useMemo(()=>{if(!action)return null;if(action.kind==='cancel')return <>Cancel <b>{action.order.symbol}</b> order <b>{action.order.orderId}</b>?</>;if(action.kind==='cancelAll')return <>Cancel <b>all {action.symbol}</b> orders on <b>{action.accountName}</b>?</>;if(action.kind==='close')return <>Close <b>{action.percent}%</b> of <b>{action.position.symbol} {action.position.side}</b> at Market with reduce-only?</>;return <>Cancel all <b>{action.position.symbol}</b> orders and close the entire <b>{action.position.side}</b> position at Market?</>},[action]);
 return <div className="page"><div className="page-head"><div><h1>Trade Control</h1><p>Real-time positions, orders and fills across Bybit accounts</p></div><div className="top-controls"><select className="select" value={account} onChange={e=>setAccount(Number(e.target.value))}><option value={0}>All accounts</option>{config.data?.accounts.map(a=><option key={a.id} value={a.id}>{a.name}{a.demo?' · demo':''}</option>)}</select><input className="input" placeholder="Symbol filter" value={symbol} onChange={e=>setSymbol(e.target.value.toUpperCase())}/><button className="btn secondary" onClick={refresh}><RefreshCw size={14}/>Refresh</button>{!live&&<span className="badge demo">actions locked</span>}</div></div><div className="account-cards">{summary.data?.map(s=><div className={account===s.accountId?'account-card card active':'account-card card'} key={s.accountId} onClick={()=>setAccount(account===s.accountId?0:s.accountId)}><h3>{s.accountName||`Account ${s.accountId}`} {s.online?<span className="badge live">online</span>:<span className="badge">offline</span>}</h3><div className="account-stat"><span>Equity</span><b>{s.online?money(s.equity):'—'}</b></div><div className="account-stat"><span>Unrealized</span><b className={s.unrealisedPnl>=0?'positive':'negative'}>{s.online?money(s.unrealisedPnl):'—'}</b></div><div className="account-stat"><span>Positions / Orders</span><b>{s.online?`${s.positions} / ${s.orders}`:'—'}</b></div></div>)}</div><div className="tabs">{tabs.map(([id,label,count])=><button key={id} className={tab===id?'tab active':'tab'} onClick={()=>setTab(id)}>{label} {count}</button>)}</div><div className="card table-card">{tab==='positions'&&<table className="data-table"><thead><tr><th>Account</th><th>Symbol</th><th>Side</th><th>Size</th><th>Entry</th><th>Mark</th><th>uPnL</th><th>SL</th><th>TP</th><th>Liq</th><th>Actions</th></tr></thead><tbody>{filter(positions.data).map(p=><tr key={`${p.accountId}-${p.symbol}-${p.positionIdx}`} onClick={()=>{setSelected(p);setSl(String(p.stopLoss||''));setTp(String(p.takeProfit||''));setTrailing(String(p.trailingStop||''))}}><td>{p.accountName}</td><td><b>{p.symbol}</b></td><td className={p.side==='Buy'?'positive':'negative'}>{p.side}</td><td>{num(p.size,6)}</td><td>{num(p.avgPrice,6)}</td><td>{num(p.markPrice,6)}</td><td className={p.unrealisedPnl>=0?'positive':'negative'}>{money(p.unrealisedPnl)}</td><td>{p.stopLoss?num(p.stopLoss,6):'—'}</td><td>{p.takeProfit?num(p.takeProfit,6):'—'}</td><td>{p.liqPrice?num(p.liqPrice,6):'—'}</td><td><div className="row-actions" onClick={e=>e.stopPropagation()}><button className="mini-btn" disabled={!live} onClick={()=>setAction({kind:'close',position:p,percent:25})}>25%</button><button className="mini-btn" disabled={!live} onClick={()=>setAction({kind:'close',position:p,percent:50})}>50%</button><button className="mini-btn danger" disabled={!live} onClick={()=>setAction({kind:'close',position:p,percent:100})}>Close</button><button className="mini-btn danger" disabled={!live} onClick={()=>setAction({kind:'flatten',position:p})}>Flatten</button></div></td></tr>)}</tbody></table>}{tab==='orders'&&<table className="data-table"><thead><tr><th>Account</th><th>Symbol</th><th>Side</th><th>Type</th><th>Status</th><th>Price</th><th>Qty</th><th>Filled</th><th>Trigger</th><th>SL</th><th>TP</th><th/></tr></thead><tbody>{filter(orders.data).map(o=><tr key={`${o.accountId}-${o.orderId}`}><td>{o.accountName}</td><td><b>{o.symbol}</b></td><td className={o.side==='Buy'?'positive':'negative'}>{o.side}</td><td>{o.orderType}</td><td>{o.orderStatus}</td><td>{num(o.price,6)}</td><td>{num(o.qty,6)}</td><td>{num(o.cumExecQty,6)}</td><td>{o.triggerPrice?num(o.triggerPrice,6):'—'}</td><td>{o.stopLoss?num(o.stopLoss,6):'—'}</td><td>{o.takeProfit?num(o.takeProfit,6):'—'}</td><td><div className="row-actions"><button className="mini-btn danger" disabled={!live} onClick={()=>setAction({kind:'cancel',order:o})}>Cancel</button><button className="mini-btn" disabled={!live} onClick={()=>setAction({kind:'cancelAll',accountId:o.accountId,accountName:o.accountName,symbol:o.symbol})}>Cancel all</button></div></td></tr>)}</tbody></table>}{tab==='executions'&&<table className="data-table"><thead><tr><th>Account</th><th>Time</th><th>Symbol</th><th>Side</th><th>Price</th><th>Qty</th><th>Fee</th><th>Order ID</th></tr></thead><tbody>{filter(executions.data).map(x=><tr key={`${x.accountId}-${x.execId}`}><td>{x.accountName}</td><td>{new Date(x.execTime).toLocaleString()}</td><td><b>{x.symbol}</b></td><td>{x.side}</td><td>{num(x.execPrice,6)}</td><td>{num(x.execQty,6)}</td><td>{num(x.execFee,6)}</td><td>{x.orderId}</td></tr>)}</tbody></table>}{tab==='history'&&<table className="data-table"><thead><tr><th>Account</th><th>Time</th><th>Symbol</th><th>Side</th><th>Type</th><th>Status</th><th>Avg / Price</th><th>Qty</th><th>Filled</th></tr></thead><tbody>{filter(history.data).map(o=><tr key={`${o.accountId}-${o.orderId}`}><td>{o.accountName}</td><td>{new Date(o.updatedTime).toLocaleString()}</td><td><b>{o.symbol}</b></td><td>{o.side}</td><td>{o.orderType}</td><td>{o.orderStatus}</td><td>{num(o.price,6)}</td><td>{num(o.qty,6)}</td><td>{num(o.cumExecQty,6)}</td></tr>)}</tbody></table>}{((tab==='positions'&&!filter(positions.data).length)||(tab==='orders'&&!filter(orders.data).length)||(tab==='executions'&&!filter(executions.data).length)||(tab==='history'&&!filter(history.data).length))&&<div className="empty">No data for the selected filters.</div>}</div>{selected&&<aside className="drawer" style={{position:'fixed',right:14,top:14,bottom:14,zIndex:60,maxHeight:'none'}}><div className="drawer-head"><div><h2>{selected.symbol} {selected.side}</h2><div className="muted">{selected.accountName} · size {num(selected.size,6)}</div></div><button className="icon-btn" onClick={()=>setSelected(null)}><X size={16}/></button></div><div className="metric-grid"><div className="metric"><small>Entry</small><strong>{num(selected.avgPrice,6)}</strong></div><div className="metric"><small>Mark</small><strong>{num(selected.markPrice,6)}</strong></div><div className="metric"><small>uPnL</small><strong className={selected.unrealisedPnl>=0?'positive':'negative'}>{money(selected.unrealisedPnl)}</strong></div><div className="metric"><small>Liquidation</small><strong>{selected.liqPrice?num(selected.liqPrice,6):'—'}</strong></div></div><div className="drawer-section"><div className="field"><label>Stop Loss</label><input className="input" type="number" step="any" value={sl} onChange={e=>setSl(e.target.value)}/></div><div className="field"><label>Take Profit</label><input className="input" type="number" step="any" value={tp} onChange={e=>setTp(e.target.value)}/></div><div className="field"><label>Trailing Stop distance</label><input className="input" type="number" step="any" value={trailing} onChange={e=>setTrailing(e.target.value)}/></div><div className="drawer-actions"><button className="btn secondary" disabled={!live||updateStops.isPending} onClick={()=>updateStops.mutate({})}>Update SL / TP / Trail</button><button className="btn secondary" disabled={!live||updateStops.isPending} onClick={()=>updateStops.mutate({stopLoss:selected.avgPrice})}>Move SL to breakeven</button><button className="btn ghost" onClick={()=>{location.href=`/?symbol=${encodeURIComponent(selected.symbol)}`}}>Open on chart</button></div></div><div className="drawer-section"><h3 style={{fontSize:12}}><AlertTriangle size={14}/> Emergency</h3><div className="drawer-actions"><button className="btn warning" disabled={!live} onClick={()=>setAction({kind:'close',position:selected,percent:50})}>Close 50%</button><button className="btn danger" disabled={!live} onClick={()=>setAction({kind:'close',position:selected,percent:100})}>Force close 100%</button><button className="btn danger" disabled={!live} onClick={()=>setAction({kind:'flatten',position:selected})}>Flatten symbol</button></div></div></aside>}<ConfirmDialog open={Boolean(action)} title={action?.kind==='flatten'?'Emergency flatten':action?.kind==='close'?'Close position':action?.kind==='cancelAll'?'Cancel all orders':'Cancel order'} body={actionBody} danger onClose={()=>setAction(null)} onConfirm={()=>action&&run.mutate(action)} confirmLabel={action?.kind==='flatten'?'FLATTEN NOW':action?.kind==='close'?'Close position':action?.kind==='cancelAll'?'Cancel all':'Cancel order'}/></div>}
