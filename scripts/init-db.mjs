import { DatabaseSync } from 'node:sqlite';import{mkdirSync}from'node:fs';import{dirname}from'node:path';
const path=process.env.DATABASE_PATH||'./data/trade.sqlite';mkdirSync(dirname(path),{recursive:true});const db=new DatabaseSync(path);db.exec(`PRAGMA journal_mode=WAL;PRAGMA foreign_keys=ON;CREATE TABLE IF NOT EXISTS system_events(id INTEGER PRIMARY KEY AUTOINCREMENT,severity TEXT NOT NULL DEFAULT 'info',event_type TEXT NOT NULL,account_id INTEGER,symbol TEXT,message TEXT NOT NULL,payload_json TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);`);console.log(`Database ready: ${path}`);
